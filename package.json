{
  "name": "CMMCworkflow",
  "version": "1.0.0",
  "description": "",
  "main": "scrape.js",
  "scripts": {
    "start": "node server.js",
    "build": "npx playwright install --with-deps",
    "postinstall": "npx playwright install --with-deps",
    "scrape": "node -e \"require('./scrape.js')().then(r => console.log('Done')).catch(console.error)\""
  },
  "private": true,
  "dependencies": {
    "express": "^5.1.0",
    "playwright": "^1.54.2"
  }
}
